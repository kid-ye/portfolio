---
import Sun from "./icons/sun.astro";
import Moon from "./icons/moon.astro";
---

<script>
  const handleToggleClick = () => {
    const element = document.documentElement;
    element.classList.toggle("dark");

    const isDark = element.classList.contains("dark");
    localStorage.setItem("theme", isDark ? "dark" : "light");
  };

  document
    .getElementById("themeToggle")
    ?.addEventListener("click", handleToggleClick);
</script>

<header>
  <div class="animate-intro mb-6 flex items-center justify-between">
    <h1
      class="text-6xl font-semibold"
      style="font-family: 'AwesomeBi_polar', sans-serif; color: green;"
    >
      YASH GUPTA
    </h1>
    <nav class="flex items-center space-x-3">
      <!-- <a href="/about" class="exclude text-neutral-500 hover:underline">About</a -->
      <!-- > -->
      <button
        id="themeToggle"
        aria-label="Toggle theme"
        class="flex size-6 items-center justify-center"
      >
        <span class="sun block text-neutral-900 dark:hidden">
          <Sun />
        </span>
        <span class="moon hidden text-neutral-100 dark:block">
          <Moon />
        </span>
      </button>
    </nav>
  </div>
  <div class="flex flex-col gap-y-3.5">
    <p
      class="animate-intro text-justify leading-snug font-light opacity-0 [animation-delay:0.2s]"
    >
      I’m an engineering student with a strong interest in systems, embedded
      hardware, and full-stack development. This is my small corner of the
      internet where I document projects, experiments, and ideas I’m actively
      working on.
    </p>
    <div
      class="animate-intro flex items-center font-light opacity-0 [animation-delay:0.25s]"
    >
      <span class="text-neutral-500 dark:text-neutral-400">Availability</span>
      <span
        class="ml-1.5 rounded border border-green-500 px-2 py-1 text-xs font-semibold text-neutral-500 dark:border-green-400 dark:text-neutral-400"
        >Open To Offers</span
      >
    </div>
    <p
      class="animate-intro text-justify font-light opacity-0 [animation-delay:0.3s]"
    >
      Outside of coursework, I spend most of my time building things—ranging
      from low-level hardware projects to data-driven web platforms—contributing
      to open source when possible, and exploring how software and electronics
      intersect in real-world systems.
    </p>
  </div>
</header>
<style>
  .sun svg,
  .moon svg {
    width: 20px;
    height: 20px;
  }
</style>
